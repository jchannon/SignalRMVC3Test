<!DOCTYPE html>
<html>
<head>
    <title>@ViewBag.Title</title>
    <link href="@Url.Content("~/Content/Site.css")" rel="stylesheet" type="text/css" />
    <script src="@Url.Content("~/Scripts/jquery-1.7.1.min.js")" type="text/javascript"></script>
    <script src="../../Scripts/jquery.signalR-1.1.3.js"></script>
    <script src="@Url.Content("~/signalr/hubs")"></script>
    <script type="text/javascript">
        hub = $.connection.myHub;

        // Assign a function to be called by the server 
        //hub.client.addOutbound = $.proxy(CConsole.OutboundController.incomingAddMessage, CConsole.OutboundController);
        //hub.client.updateOutbound = $.proxy(CConsole.OutboundController.updateMessage, CConsole.OutboundController);
        //hub.client.removeOutbound = $.proxy(CConsole.OutboundController.incomingRemoveMessage, CConsole.OutboundController);

        //hub.client.addMessage = $.proxy(CConsole.MessageController.incomingMessage, CConsole.MessageController);
        //hub.client.updateMessage = $.proxy(CConsole.MessageController.updateMessage, CConsole.MessageController);
        //hub.client.removeMessage = $.proxy(CConsole.MessageController.removeMessage, CConsole.MessageController);

        // Start the connection
        $.connection.hub.start().done(function () {
            console.log('Persistant connection started. ConnectionId:' + $.connection.hub.id);
        });
    </script>
</head>

<body>
    @RenderBody()
</body>
</html>
